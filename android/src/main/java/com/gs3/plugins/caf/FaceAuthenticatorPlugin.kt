package com.gs3.plugins.caf

import android.content.Context
import android.content.Intent
//import com.combateafraude.faceauthenticator.FaceAuthenticatorActivity
//import com.combateafraude.faceauthenticator.input.FaceAuthenticator


class FaceAuthenticatorPlugin(context: Context) {
 /*    val context = context;
    private fun build(jwt: String, cpf: String): FaceAuthenticator {
        return FaceAuthenticator.Builder(jwt)
            .setPeopleId(cpf)
            .setUseEmulator(true)
            .setUseRoot(true)
            .setUseDeveloperMode(true)
            .setUseAdb(true)
            .setUseDebug(true)
            .build();
    }

    fun faceAuthenticator(jwt: String, cpf: String): Intent {
        val faceAuthenticator: FaceAuthenticator = this.build(jwt, cpf)
        val mIntent = Intent(context, FaceAuthenticatorActivity::class.java)
        mIntent.putExtra(FaceAuthenticator.PARAMETER_NAME, faceAuthenticator)
        return mIntent;
    }
 */

}